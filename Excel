# Delete the file locally
git rm excel

# Commit the deletion with a meaningful message
git commit -m "Remove Excel file from main branch. It is no longer needed."

# Push the changes to GitHub
git push origin main


